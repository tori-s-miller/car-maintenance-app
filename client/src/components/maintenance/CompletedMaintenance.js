import React, { Fragment, useState } from 'react';
import { connect } from 'react-redux';

const CompletedMaintenance = () => {
    return (
        <Fragment>
            
        </Fragment>
    )
}

export default connect(null)(CompletedMaintenance);